(() => {
  var __defProp = Object.defineProperty;
  var __getOwnPropSymbols = Object.getOwnPropertySymbols;
  var __hasOwnProp = Object.prototype.hasOwnProperty;
  var __propIsEnum = Object.prototype.propertyIsEnumerable;
  var __defNormalProp = (obj, key, value) => key in obj ? __defProp(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
  var __spreadValues = (a, b) => {
    for (var prop in b || (b = {}))
      if (__hasOwnProp.call(b, prop))
        __defNormalProp(a, prop, b[prop]);
    if (__getOwnPropSymbols)
      for (var prop of __getOwnPropSymbols(b)) {
        if (__propIsEnum.call(b, prop))
          __defNormalProp(a, prop, b[prop]);
      }
    return a;
  };
  var __async = (__this, __arguments, generator) => {
    return new Promise((resolve, reject) => {
      var fulfilled = (value) => {
        try {
          step(generator.next(value));
        } catch (e) {
          reject(e);
        }
      };
      var rejected = (value) => {
        try {
          step(generator.throw(value));
        } catch (e) {
          reject(e);
        }
      };
      var step = (x) => x.done ? resolve(x.value) : Promise.resolve(x.value).then(fulfilled, rejected);
      step((generator = generator.apply(__this, __arguments)).next());
    });
  };

  // src/js/include/config.js
  /**
  * @preserve
  * Filename: config.js
  *
  * Created: 21/07/2025 (14:15:10)
  * Created by: Lorenzo Saibal Forti <lorenzo.forti@gmail.com>
  *
  * Last update: 12/09/2025 (10:51:43)
  * Updated by: Lorenzo Saibal Forti <lorenzo.forti@gmail.com>
  *
  * Copyleft: 2025 - Tutti i diritti riservati
  *
  * Comments:
  */
  var defaultConfig = {
    "targetInput": "#js-password",
    "titleText": "",
    "ariaLabelPrefix": "visibilit\xE0 campo $:",
    "ariaLabelShow": "visibile",
    "ariaLabelHide": "nascosta",
    "liveTextShow": "il testo del campo $ \xE8 visibile",
    "liveTextHide": "il testo del campo $ \xE8 nascosto",
    "liveRegionId": "js-showpassword-live-region",
    "backgroundColor": "#fff",
    "iconShow": `
		<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="none" part="icon-show">
			<path part="icon-show-path" stroke="#141414" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M2.42 12.713c-.136-.215-.204-.323-.242-.49a1.173 1.173 0 0 1 0-.446c.038-.167.106-.274.242-.49C3.546 9.505 6.895 5 12 5s8.455 4.505 9.58 6.287c.137.215.205.323.243.49.029.125.029.322 0 .446-.038.167-.106.274-.242.49C20.455 14.495 17.105 19 12 19c-5.106 0-8.455-4.505-9.58-6.287Z"/>
			<path part="icon-show-path" stroke="#141414" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 15a3 3 0 1 0 0-6 3 3 0 0 0 0 6Z"/>
		</svg>`,
    "iconHide": `
		<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="none" part="icon-hide">
			<path part="icon-hide-path" stroke="#141414" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10.743 5.092C11.149 5.032 11.569 5 12 5c5.105 0 8.455 4.505 9.58 6.287.137.215.205.323.243.49.029.125.029.322 0 .447-.038.166-.107.274-.244.492-.3.474-.757 1.141-1.363 1.865M6.724 6.715c-2.162 1.467-3.63 3.504-4.303 4.57-.137.217-.205.325-.243.492a1.173 1.173 0 0 0 0 .446c.038.167.106.274.242.49C3.546 14.495 6.895 19 12 19c2.059 0 3.832-.732 5.289-1.723M3 3l18 18M9.88 9.879a3 3 0 1 0 4.243 4.243"/>
		</svg>`
  };

  // src/js/include/setStyle.js
  var setStyle = () => {
    const stylesheet = new CSSStyleSheet();
    stylesheet.replaceSync(`

		:host,
		:host *,
		:host *::before,
		:host *::after
		{
			box-sizing: border-box;
		}

		.btn-showpassword
		{
			position: absolute;
			top: 0.25rem;
			bottom: 0.25rem;
			right: 0;
			padding-inline: 0.5rem;
			display: flex;
  			align-items: center;
  			justify-content: center;
			background: var(--bg-color, #fff);
			border-width: 0;
			margin-right: 0.5rem;
			cursor: pointer;
		}

		.btn-showpassword:focus-visible
		{
  			outline: none;
  			box-shadow: 0 0 0 2px #666666bf;
			border-radius: 0.25rem;
		}
	`);
    return stylesheet;
  };

  // src/js/include/util.js
  /**
  * @preserve
  * Filename: util.js
  *
  * Created: 21/07/2025 (15:53:26)
  * Created by: Lorenzo Saibal Forti <lorenzo.forti@gmail.com>
  *
  * Last update: 04/09/2025 (11:05:50)
  * Updated by: Lorenzo Saibal Forti <lorenzo.forti@gmail.com>
  *
  * Copyleft: 2025 - Tutti i diritti riservati
  *
  * Comments:
  */
  var isInlineSvg = (data) => {
    return typeof data === "string" && data.trim().startsWith("<svg");
  };
  var setLiveRegion = (context) => {
    const regionId = context.config.liveRegionId;
    if (!regionId) {
      console.log("Missing region Id");
      return false;
    }
    if (!document.getElementById(regionId)) {
      const region = document.createElement("div");
      region.id = regionId;
      region.setAttribute("aria-live", "polite");
      region.setAttribute("aria-atomic", "true");
      region.setAttribute("role", "status");
      region.style.cssText = "position:absolute;clip:rect(0 0 0 0);clip-path:inset(50%);width:1px;height:1px;margin:-1px;border:0;overflow:hidden;white-space:nowrap;";
      document.body.appendChild(region);
    }
    context.liveRegion = document.getElementById(regionId);
    return context.liveRegion;
  };
  var toggleRegionText = (context, isInputPassword) => {
    var _a;
    setLiveRegion(context);
    if (!context.liveRegion) return;
    const inputLabel = (_a = getLabelInput(context)) != null ? _a : "";
    const liveText = isInputPassword ? context.liveTextShow.replace("$", inputLabel) : context.liveTextHide.replace("$", inputLabel);
    clearTimeout(context.liveTimer);
    context.liveTimer = setTimeout(() => {
      context.liveRegion.textContent = liveText;
    }, 200);
  };
  var checkTarget = (context, target) => {
    if (!target) {
      context.style.setProperty("display", "none");
      console.log("showPassword component: missing target id component");
      return false;
    }
    return true;
  };
  var getLabelInput = (context) => {
    var _a;
    if (context.labelInput) {
      return context.labelInput.trim();
    }
    const input = document.querySelector(context.targetInput);
    if (!input) return null;
    let label = (_a = input.labels) == null ? void 0 : _a[0];
    if (!label && input.id) {
      label = document.querySelector(`label[for="${CSS.escape(input.id)}"]`);
    }
    return (label == null ? void 0 : label.textContent.trim()) || null;
  };
  var getImageList = (context) => {
    const imageList = {
      "iconShow": context.config.iconShow,
      "iconHide": context.config.iconHide
    };
    if (context.iconShow) {
      imageList.iconShow = context.iconShow;
    }
    if (context.iconHide) {
      imageList.iconHide = context.iconHide;
    }
    return imageList;
  };
  var setIcon = (button, icon) => {
    if (isInlineSvg(icon)) {
      button.innerHTML = icon;
    } else {
      const existImg = button.querySelector("img[part='icon']");
      if (existImg) {
        existImg.src = icon;
      } else {
        const img = new Image();
        img.src = icon;
        img.setAttribute("part", "icon");
        img.setAttribute("role", "presentation");
        img.setAttribute("alt", "");
        button.replaceChildren(img);
      }
    }
  };
  var handleClick = (e, context) => __async(null, null, function* () {
    if (typeof context.callbackBefore === "function") {
      yield context.callbackBefore(e);
    }
    const isInputPassword = context.targetElem.getAttribute("type") === "password";
    const inputType = isInputPassword ? "text" : "password";
    const ariaLabelStatus = isInputPassword ? context.statusTextShow : context.statusTextHide;
    const currentIcon = isInputPassword ? context.iconList.iconHide : context.iconList.iconShow;
    const ariaLabelPrefix = context.config.ariaLabelPrefix.replace("$", context.inputLabel);
    context.targetElem.setAttribute("type", inputType);
    context.buttonShowPassword.setAttribute("aria-label", `${ariaLabelPrefix} ${ariaLabelStatus}`);
    context.buttonShowPassword.setAttribute("aria-pressed", isInputPassword);
    setIcon(context.buttonShowPassword, currentIcon);
    toggleRegionText(context, isInputPassword);
    if (typeof context.callbackAfter === "function") {
      yield context.callbackAfter(e);
    }
  });

  // src/js/include/initShadowDom.js
  /**
  * @preserve
  * Filename: initShadowDom.js
  *
  * Created: 21/07/2025 (14:15:03)
  * Created by: Lorenzo Saibal Forti <lorenzo.forti@gmail.com>
  *
  * Last update: 12/09/2025 (10:51:57)
  * Updated by: Lorenzo Saibal Forti <lorenzo.forti@gmail.com>
  *
  * Copyleft: 2025 - Tutti i diritti riservati
  *
  * Comments:
  */
  var initShadowDom = (context) => {
    const idButton = "js-button-show-password";
    const ariaLabelPrefix = context.config.ariaLabelPrefix.replace("$", context.inputLabel);
    context.attachShadow({
      "mode": "open"
    });
    context.shadowRoot.adoptedStyleSheets = [setStyle()];
    context.shadowRoot.innerHTML = `

		<button id="${idButton}" class="btn-showpassword" part="btn-showpassword" aria-pressed="false" aria-label="${ariaLabelPrefix} ${context.statusTextHide}">
			${context.config.iconShow}
		</button>
	`;
    context.buttonShowPassword = context.shadowRoot.getElementById(idButton);
    if (context.iconShow) {
      setIcon(context.buttonShowPassword, context.iconList.iconShow);
    }
  };

  // src/js/show-password.js
  /**
  * @preserve
  * Filename: show-password.js
  *
  * Created: 05/08/2025 (16:06:21)
  * Created by: Lorenzo Saibal Forti <lorenzo.forti@gmail.com>
  *
  * Last update: 04/09/2025 (11:07:14)
  * Updated by: Lorenzo Saibal Forti <lorenzo.forti@gmail.com>
  *
  * Copyleft: 2025 - Tutti i diritti riservati
  *
  * Comments:
  */
  var ShowPassword = class extends HTMLElement {
    constructor() {
      var _a, _b;
      super();
      this.config = __spreadValues(__spreadValues({}, defaultConfig), (_a = window.showPasswordConfig) != null ? _a : {});
      this.handleClick = (e) => handleClick(e, this);
      this.callbackBefore = null;
      this.callbackAfter = null;
      this.liveTimer = null;
      this.iconList = getImageList(this);
      this.inputLabel = (_b = getLabelInput(this)) != null ? _b : "";
      initShadowDom(this);
    }
    // setter/getter per cbBefore
    set cbBefore(fn) {
      if (typeof fn === "function") {
        this.callbackBefore = fn;
      } else {
        this.callbackBefore = null;
      }
    }
    get cbBefore() {
      return this.callbackBefore;
    }
    // setter/getter per cbAfter
    set cbAfter(fn) {
      if (typeof fn === "function") {
        this.callbackAfter = fn;
      } else {
        this.callbackAfter = null;
      }
    }
    get cbAfter() {
      return this.callbackAfter;
    }
    // get attributi locali del component
    get backgroundColor() {
      return this.getAttribute("background-color") || this.config.backgroundColor;
    }
    get targetInput() {
      return this.getAttribute("target-input") || this.config.targetInput;
    }
    get labelInput() {
      return this.getAttribute("label-input");
    }
    get iconShow() {
      return this.getAttribute("icon-show");
    }
    get iconHide() {
      return this.getAttribute("icon-hide");
    }
    get titleText() {
      return this.getAttribute("title") || this.config.titleText;
    }
    get statusTextShow() {
      return this.getAttribute("status-text-show") || this.config.ariaLabelShow;
    }
    get statusTextHide() {
      return this.getAttribute("status-text-hide") || this.config.ariaLabelHide;
    }
    get liveTextShow() {
      return this.getAttribute("live-text-show") || this.config.liveTextShow;
    }
    get liveTextHide() {
      return this.getAttribute("live-text-hide") || this.config.liveTextHide;
    }
    // get attributi locali del component
    connectedCallback() {
      this.targetElem = document.querySelector(this.targetInput);
      checkTarget(this, this.targetElem);
      this.setupComponent();
      this.buttonShowPassword.addEventListener("click", this.handleClick);
    }
    disconnectedCallback() {
      this.callbackBefore = null;
      this.callbackAfter = null;
      this.buttonShowPassword.removeEventListener("click", this.handleClick);
    }
    setupComponent() {
      this.style.setProperty("--bg-color", this.backgroundColor);
      if (this.titleText) {
        this.buttonShowPassword.setAttribute("title", this.titleText);
        this.removeAttribute("title");
      }
    }
  };
  if (!customElements.get("show-password")) {
    customElements.define("show-password", ShowPassword);
  }
})();
//# sourceMappingURL=show-password.min.js.map
